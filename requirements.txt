# 运行依赖
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0.30
pydantic>=2.7
cryptography>=42.0
httpx>=0.27

# 生产数据库驱动（本地开发用 aiosqlite）
asyncpg>=0.29
aiosqlite>=0.20

# 可选：Redis 幂等存储。不装则自动降级到进程内实现
redis>=5.0

# 测试
pytest>=8.0
